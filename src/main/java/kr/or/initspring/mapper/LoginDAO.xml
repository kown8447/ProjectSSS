<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.initspring.dao.LoginDAO">
	<select id="getRoleByUserid" resultType="String">
		select role_name from role where userid = #{userid}
	</select>
	
	<select id="searchID" resultType="String">
		select userid from membertest where name=#{param1} and email=#{param2}
	</select>
	
	<select id="getMemberTempByUserid" resultType="int">
		select member_temp from membertest where userid=#{userid}	
	</select>
	
	<select id="isValidID" resultType="int">
		select count(*) from membertest where userid = #{userid}
	</select>
	
	<select id="getPwdByUserid" resultType="String" >
		select pwd from membertest where userid = #{userid}
	</select>
	
	<update id="updatePwd">
		update membertest set pwd = #{param2}, member_temp=1 where userid = #{param1}
	</update>
	
	<select id="getEmailByUserid" resultType="String">
		select email from membertest where userid = #{userid}
	</select>
</mapper>